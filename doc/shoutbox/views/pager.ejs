<div id='pager'>
  <% if (page.count > 1) { %>
    <% if (page.number) { %>
      <a id='prev' href='/entry?page=<%= page.number %>'>Prev</a>
      <% } %>
        <% if (page.number < page.count - 1) { %>
          <% if (page.number) { %>
        <% } %>
        <a id='next' href='/entry?page=<%= page.number + 2 %>'>Next</a>
      <% } %>
    <% } %>
</div>